apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-node-charts.fullname" . }}
  labels:
    {{- include "hello-node-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-node-charts.selectorLabels" . | nindent 4 }}
