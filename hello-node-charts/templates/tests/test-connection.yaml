apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-node-charts.fullname" . }}-test-connection"
  labels:
    {{- include "hello-node-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-node-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
